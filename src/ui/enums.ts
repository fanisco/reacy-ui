export enum Styles {
    default = 'default',
    success = 'success',
    danger = 'danger',
    primary = 'primary',
    secondary = 'secondary'
}

export enum Sizes {
    xs = 'xs', sm = 'sm', md = 'md', lg = 'lg', xl = 'xl'
}
