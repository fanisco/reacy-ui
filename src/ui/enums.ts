export enum Styles {
    default = 'default',
    success = 'success',
    danger = 'danger',
    warning = 'warning'
}

export enum Sizes {
    xs = 'xs', sm = 'sm', md = 'md', lg = 'lg', xl = 'xl'
}
