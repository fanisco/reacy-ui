export enum Styles {
    default = 'default',
    success = 'success',
    warning = 'warning',
    primary = 'primary',
    secondary = 'secondary'
}

export enum Sizes {
    xs = 'xs', sm = 'sm', md = 'md', lg = 'lg', xl = 'xl'
}
